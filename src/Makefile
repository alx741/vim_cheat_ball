# This Makefile was generated by the made script:
# https://github.com/alx741/made
#
# Replace OUTPUTS with your output PDFs
#
# Dependencies:
# 	Inkscape


INPUTS = $(shell find . -type f -name '*.svg')
OUTPUTS = $(foreach input, $(INPUTS), $(input:.svg=.pdf))

all: $(OUTPUTS)

%.pdf: %.svg
	inkscape -C $< -o $@

clean:
	rm $(OUTPUTS)
