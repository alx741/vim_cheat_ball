# This Makefile was generated by the made script:
# https://github.com/alx741/made
#
# Replace OUTPUTS with your output PNGs
#
# Dependencies:
# 	Inkscape


INPUTS = $(shell find . -type f -name '*.svg')
OUTPUTS = $(foreach input, $(INPUTS), $(input:.svg=.png))

all: $(OUTPUTS)

%.png: %.svg
	inkscape -D $< -o $@

clean:
	rm $(OUTPUTS)
